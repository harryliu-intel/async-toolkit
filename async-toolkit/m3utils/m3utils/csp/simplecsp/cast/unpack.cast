module unpack;

define P()()
{
  csp {
    structure struct0 =
      (int(8) a = 1;
       int(8) b = 1, d = 2;
       int(8) c = 13;
       );
    
  struct0 x;
  int y;

  x::a = 2;
  
  y = pack(x);

  print ("y = " + y);
  
  }
  
}
