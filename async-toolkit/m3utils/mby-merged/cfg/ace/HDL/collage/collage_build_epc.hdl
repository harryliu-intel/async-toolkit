$hdl_spec = {
# Assumed to be in the same directory as the .hdl file
  -collage_build_file => [
                          "$ENV{MODEL_ROOT}/tools/collage/epc/build/builder.epc.tcl",
                       ],
  -collage_build_design =>  'epc' ,
};
