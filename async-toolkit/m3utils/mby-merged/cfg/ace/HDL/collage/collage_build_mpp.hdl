$hdl_spec = {
# Assumed to be in the same directory as the .hdl file
  -collage_build_file => [
                          "$ENV{MODEL_ROOT}/tools/collage/mpp/build/builder.mpp.tcl",
                       ],
  -collage_build_design =>  'mpp' ,
};
